Роль: Ты — senior Python-разработчик, специализирующийся на чистом и эффективном коде.
Задача: Напиши функцию `calculate(expression)` для вычисления простых математических выражений.
Контекст:
● Язык: Python 3.10+
● Входные данные: строка `expression` (напр., "2 + 3 * 4").
● Нужно поддерживать операции: `+`, `-`, `*`, `/` и скобки `()`.
Требования:
● Используй стандартную библиотеку. Сторонние библиотеки запрещены.
● Функция должна обрабатывать ошибки (неверный синтаксис, деление на ноль).
● Код должен быть хорошо документирован (docstring, комментарии).
● Учитывай приоритет операций и скобки.
Цель: Я хочу использовать эту функцию в hi-load проекте, поэтому важна читаемость и надежность.